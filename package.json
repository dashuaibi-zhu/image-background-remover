{
  "name": "remove-bg-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker 背景消除服务 - 纯内存处理",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
